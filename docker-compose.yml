version: '3.7'

services:
  spark-master:
    image: 192.168.1.15:5000/spark-master
    ports:
      - "9090:8080"
      - "7077:7077"
    volumes:
       - /home/babloo/work/git/docker-spark-cluster:/opt/spark-apps
       - /home/babloo/work/git/docker-spark-cluster/opt/spark-data
    networks:
      - spark-network

  spark-worker:
    image: 192.168.1.15:5000/spark-worker
    depends_on:
      - spark-master
    environment:
      - SPARK_MASTER=spark://spark-master:7077
      - SPARK_WORKER_CORES=4
      - SPARK_WORKER_MEMORY=2G
      - SPARK_DRIVER_MEMORY=128m
      - SPARK_EXECUTOR_MEMORY=256m
    volumes:
       - /home/babloo/work/git/docker-spark-cluster:/opt/spark-apps
       - /home/babloo/work/git/docker-spark-cluster:/opt/spark-data
    networks:
      - spark-network
    deploy:
      mode: replicated
      replicas: 5
  
networks:
  spark-network:
